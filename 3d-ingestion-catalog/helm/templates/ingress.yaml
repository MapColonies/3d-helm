{{ if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-3d-ingetion-catalog-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - http:
      paths:
      - path: /{{ .Release.Name }}/3d-ingetion-catalog(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: {{ .Release.Name }}-3d-ingetion-catalog-service
            port:
              number: 8080
{{ end }}