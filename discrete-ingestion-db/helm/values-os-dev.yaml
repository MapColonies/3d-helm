
discreteIngestionDb:
  # The port on which the service should listen
  port: 8080

  # Name of the image tu pull from registry
  image: acrarolibotnonprod.azurecr.io/discrete-ingestion-db:dev-3d

  db:
    host: localhost
    port: 5432
    username: postgres
    password: postgres
    database: postgres

ingress:
  enabled: false

route:
  enabled: true