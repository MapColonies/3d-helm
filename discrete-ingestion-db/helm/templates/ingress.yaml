{{ if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-discrete-ingestion-db-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - http:
      paths:
      - path: /{{ .Release.Name }}-discrete-ingestion-db(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: {{ .Release.Name }}-discrete-ingestion-db-service
            port:
              number: 8080
{{ end }}